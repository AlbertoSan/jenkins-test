apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-shipedge-app
  namespace: jenkins
spec:
  replicas: 1
  selector:
    matchLabels:
      app: jenkins-test
  template:
    metadata:
      labels:
        app: jenkins-test
    spec:
      containers:
      - name: jenkins-test-container
        image: i1pro/hello-world-app:latest
        ports:
        - containerPort: 3000
